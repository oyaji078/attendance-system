BACKEND_BASE_URL=http://127.0.0.1:8000
DEVICE_CODE=gate-a01
SESSION_CODE=
HEARTBEAT_INTERVAL_SECONDS=10

# Frame source: "filesystem" (default) or "camera"
# Filesystem mode:
SAMPLE_FRAME_PATH=./sample-frame.jpg
# Camera mode (requires `camera` feature):
CAMERA_INDEX=
CAMERA_WIDTH=640
CAMERA_HEIGHT=480
CAMERA_FPS=15

LIVENESS_THRESHOLD=0.70
QUEUE_MAX_SIZE=128
RETRY_BASE_BACKOFF_MS=1000
RETRY_MAX_BACKOFF_MS=60000
MAX_RETRIES=10
REQUEST_TIMEOUT_SECONDS=15
MAX_QUEUE_AGE_SECONDS=300
